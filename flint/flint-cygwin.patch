--- origsrc/flint-2.4.5/Makefile.in	2015-02-17 15:56:55.000000000 +0100
+++ Makefile.in	2015-03-23 06:40:50.543949100 +0100
@@ -169,10 +169,12 @@ else
 endif
 
 install: library
+	mkdir -p $(DESTDIR)$(PREFIX)/bin
 	mkdir -p $(DESTDIR)$(PREFIX)/lib
 	mkdir -p $(DESTDIR)$(PREFIX)/include/flint
 	$(AT)if [ "$(FLINT_SHARED)" -eq "1" ]; then \
-		cp $(FLINT_LIB) $(DESTDIR)$(PREFIX)/lib; \
+		cp $(FLINT_LIB) $(DESTDIR)$(PREFIX)/bin; \
+		cp $(FLINT_IMPLIB) $(DESTDIR)$(PREFIX)/lib; \
 	fi
 	$(AT)if [ "$(FLINT_STATIC)" -eq "1" ]; then \
 		cp libflint.a $(DESTDIR)$(PREFIX)/lib; \
--- origsrc/flint-2.4.5/configure	2015-02-17 15:56:55.000000000 +0100
+++ configure	2015-03-23 06:35:03.376092200 +0100
@@ -390,8 +390,14 @@ if [ -z "$FLINT_LIB" ]; then
       Darwin)
          FLINT_LIB="libflint.dylib"
          EXTRA_SHARED_FLAGS="-install_name $PREFIX/lib/$FLINT_LIB";;
-      CYGWIN* | MINGW*)
-         FLINT_LIB="libflint.dll";;
+      MINGW*)
+         FLINT_LIB="libflint.dll"
+         FLINT_IMPLIB="libflint.dll.a"
+         EXTRA_SHARED_FLAGS="-Wl,--out-implib=$FLINT_IMPLIB";;
+      CYGWIN*)
+         FLINT_LIB="cygflint.dll"
+         FLINT_IMPLIB="libflint.dll.a"
+         EXTRA_SHARED_FLAGS="-Wl,--out-implib=$FLINT_IMPLIB";;
       *)
          FLINT_LIB="libflint.so";;
    esac
@@ -615,6 +621,7 @@ echo "" >> Makefile
 echo "FLINT_STATIC=$STATIC" >> Makefile
 echo "FLINT_SHARED=$SHARED" >> Makefile
 echo "FLINT_LIB=$FLINT_LIB" >> Makefile
+echo "FLINT_IMPLIB=$FLINT_IMPLIB" >> Makefile
 echo "EXEEXT=$EXEEXT" >> Makefile
 echo "PREFIX=$PREFIX" >> Makefile
 echo "" >> Makefile
--- origsrc/flint-2.4.5/configure	2015-04-22 23:28:02.857865800 +0200
+++ configure	2015-04-23 00:20:59.456006000 +0200
@@ -5,12 +5,12 @@
 # (C) 2012, William Hart, Jean-Pierre Flori, Thomas DuBuisson
 # (C) 2012, Jan Engelhardt
 
-PREFIX="/usr/local"
-GMP_DIR="/usr/local"
-MPFR_DIR="/usr/local"
-NTL_DIR="/usr/local"
-GC_DIR="/usr/local"
-BLAS_DIR="/usr/local"
+PREFIX="/usr/"
+GMP_DIR="/usr/"
+MPFR_DIR="/usr/"
+NTL_DIR="/usr/"
+GC_DIR="/usr/"
+BLAS_DIR="/usr/"
 WANT_NTL=0
 WANT_BLAS=0
 SHARED=1
@@ -251,7 +251,7 @@ if [ "$WANT_BLAS" = "1" ]; then
    fi
    EXTRA_INC_DIRS="${EXTRA_INC_DIRS} ${BLAS_INC_DIR}"
    EXTRA_LIB_DIRS="${EXTRA_LIB_DIRS} ${BLAS_LIB_DIR}"
-   EXTRA_LIBS="${EXTRA_LIBS} openblas"
+   EXTRA_LIBS="${EXTRA_LIBS} blas"
 fi
 CONFIG_BLAS="#define HAVE_BLAS ${WANT_BLAS}"
 
